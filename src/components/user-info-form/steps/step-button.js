import React from 'react';
import Button from '../../common/button/button';

const StepButton = (props) => {
  return (
    <Button size={Button.Size.SMALL } {...props}/>
  );
};

export default StepButton;
